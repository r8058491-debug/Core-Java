package neeraj;

public class CurrentAccount extends Balance Account {
	
	private final double Transaction fee = 10 ;
	
	public current Account (String account string owner double ) {
		
		super (account , owner , balance ) ;
		
		public void withdraw (double amount )
		 
	throw invalid Account Exception Insuffcient Balance Exception 
	
	if (amount = 0)

		throw new Invalid Account Exception ('invalid withdrawal amount ');
	
		double total = amount + Transaction - fee;
		
		if (balance < total )
			
			throw new Insuffcient Balance Exception ("not enough balance for withdrawal + fee');
					
					balance - = total ;
					
	}
	
			public void perform Transaction () {
				
				System.out.println("current accouny transaction exception ");
				
			}